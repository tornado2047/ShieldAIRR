% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdr3_physchem.R
\name{shield_cdr3_landscape}
\alias{shield_cdr3_landscape}
\title{CDR3 physicochemical landscape analysis}
\usage{
shield_cdr3_landscape(
  df,
  sample_name = "Sample",
  output_prefix = "CDR3_Physicochemical_Landscape"
)
}
\arguments{
\item{df}{data.frame，至少包含列 \code{junction_aa} 和 \code{duplicate_count}。}

\item{sample_name}{字符串，用于图标题和输出。}

\item{output_prefix}{输出文件前缀（不含扩展名）。如果为 \code{NULL} 则不保存文件。}
}
\value{
patchwork 拼接的 ggplot 对象（invisible 返回）。
}
\description{
给定 AIRR 风格的数据框（含 \code{junction_aa} / \code{duplicate_count}），
计算 CDR3 的多种理化性质、Atchley/Kidera 因子，并拼成综合图。
}
\details{
依赖外部包 \code{sumrep}，该包未在 CRAN 上发布，需要用户自行安装或
在当前会话中通过 \code{devtools::load_all()} 加载本地版本。
}
