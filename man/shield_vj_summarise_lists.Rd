% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bulk_vj_deseq.R
\name{shield_vj_summarise_lists}
\alias{shield_vj_summarise_lists}
\title{Summarise V/J usage for two groups of samples (list vs list)}
\usage{
shield_vj_summarise_lists(
  list_case,
  list_control,
  gene = c("v_call", "j_call"),
  method = c("sum", "max", "mean", "ratio"),
  case_label = "Patient",
  control_label = "Control"
)
}
\arguments{
\item{list_case}{实验组样本列表，每个元素是一个 data.frame（一个样本）}

\item{list_control}{对照组样本列表，每个元素是一个 data.frame（一个样本）}

\item{gene}{"v_call" 或 "j_call"}

\item{method}{汇总方式：\code{"sum"}, \code{"max"}, \code{"mean"}, \code{"ratio"}}

\item{case_label}{实验组标签（默认 "Patient"）}

\item{control_label对照组标签（默认}{"Control"）}
}
\value{
list，包含：
\item{count_mat}{基因 × 样本 的矩阵（行为基因，列为样本）}
\item{condition}{因子向量，长度等于列数，对应 Control / Patient}
}
\description{
Summarise V/J usage for two groups of samples (list vs list)
}
