% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bulk_vj_deseq.R
\name{shield_vj_summarise}
\alias{shield_vj_summarise}
\title{Summarise V/J gene usage across multiple samples}
\usage{
shield_vj_summarise(
  TRA_list,
  gene = c("v_call", "j_call"),
  method = c("sum", "max", "mean", "ratio"),
  count_col = "duplicate_count"
)
}
\arguments{
\item{TRA_list}{命名的 data.frame 列表，每个元素是一份样本的 TCR/BCR 表。}

\item{gene}{选择 "v_call" 或 "j_call"。}

\item{method}{"sum", "max", "mean", "ratio" 之一。}

\item{count_col}{表示丰度的列，默认 "duplicate_count"。}
}
\value{
一个列表：\code{list(summary_list = ..., count_matrix = ..., gene = gene, method = method)}
}
\description{
给定一个样本列表（每个样本是 AIRR 风格的数据框），对 V/J 基因做
sum / max / mean / top:bottom ratio 等汇总，并返回：
\itemize{
\item \code{summary_list}: 每个样本一个 data.frame，含 \code{gene} 和 \code{stat}
\item \code{count_matrix}: 行为基因，列为样本的矩阵，可直接用于 DESeq2
}
}
